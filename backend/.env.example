PORT=5000
NODE_ENV=development
MONGODB_URI=mongodb://127.0.0.1:27017/stayy

# Comma-separated origins for the Vite dev server (optional; defaults to permissive CORS)
# CORS_ORIGIN=http://localhost:5173

# Min 16 chars in production
JWT_SECRET=change-me-to-a-long-random-secret
JWT_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=12
